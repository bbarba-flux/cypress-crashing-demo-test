{
  "devDependencies": {
    "cypress": "^13.3.3"
  },
  "scripts": {
    "open": "cypress open",
    "run": "cypress run --browser chrome",
    "run:saveLogs": "DEBUG=cypress*memory cypress run --browser chrome"
  },
  "type": "module"
}
